# moonbao · 开发计划

> 一个住在你手机夜空里的小月亮：会眨眼、会发光、会记得你说过的话、会让星星拼字回应你。
> **每次继续开发时：读本文件 → 找到第一个未完成阶段 → 做完 todo → 更新勾选 → 到 CHECKPOINT 停下请 human 验收。**
> 首要原则：**可爱度优先，核心未验收前不堆内容。** 三个侧重点按序：动画 / 可爱度 → 低延迟 → 记忆与主动性。
> 设定与想法的完整记录在 `docs/design.md`；本文件只放执行。

## 图例
- `[ ]` 待做  `[x]` 完成  `[~]` 部分完成（注明缺口）  `[?]` 设计未定，需 human 拍板
- ⏸ CHECKPOINT = 暂停等 human 验收（≤5 问，5 分钟内能做完，夹具由 agent 准备）

## 分工
技术选型与实现由 agent 负责并把关；人格设定、美术方向、体验验收和产品 idea 由用户负责。每个阶段的验收标准都是"体感"标准——只有用户能判定通过与否。

---

## P0 · 会呼吸的月亮（可爱度灭火）
目标：一个没有四肢、不会形变的球，靠眼神 + 运动曲线 + 光，让人觉得可爱。这是整个项目最大的风险，先灭火。
Definition of Done：
- three.js 月亮渲染器实现 `CharacterRenderer`，替换 DOM 占位；桌面 60fps、手机 Safari ≥30fps
- 全部可调数值集中在 `web/moon/params.ts` 并加注释（眼睛位置/间距、眨眼节奏、squash 幅度、ζ、漂浮幅度与周期、地照强度曲线……）
- 无头验收管线可用：`__step(秒)` + `__snapSave()` 落图到 `snaps/`
- 12 个动作原语全部有实现（可以粗糙，但每个都能一眼读懂）

- [x] 球 + 两颗豆眼（位置 / 间距 / 大小滑杆，参数外置）；眼睛贴在球面上永远朝向观察者，本体可独立旋转
- [x] 眨眼系统：指数分布间隔 + 双眨 / 慢眨 / 眯眼 ^ ^ / 呆眼 · · 四种形态，活力偏置频率、心情偏置眯眼
- [x] 弹簧运动：位置 / 旋转 / 缩放三条弹簧，ζ 滑杆（默认 0.5）；idle 漂浮用 value noise
- [x] squash & stretch（±4%）：速度拉伸 + bounce 落地脉冲；「被戳」要等触摸输入（P1 后）
- [x] 月相光照：自写 shader（方向光按相位角转 + 地照随相位反向 + fresnel 边缘光 + 表面斑驳）+ 眼睛在暗部泛灰蓝
- [x] 12 个动作原语全部有实现（`web/moon/actions.ts`，每个是 (t, intensity) → 姿态目标的纯函数）
- [x] 主动运动的 anticipation：lean_in 先往后缩 0.18s
- [x] 占位背景：按钟点插值的三色渐变（WebGL 里画，截图 / GIF 自带天空；真正的太阳高度角在 P5）
- [x] 空间感粗版：离轴投影相机 + 三层远星（空气透视）+ 前景尘埃 + 鼠标模拟倾斜 + 面板「自动摇」+ 光晕
- [x] lean_in 高档 = 特写（穿过玻璃、转头、一只眼盯着你）；drift_away 高档 = 飞远变小。面板有「特写」「飞远」按钮
- [x] 调试面板：情绪 / 月相 / 钟点 / 眼睛三滑杆 / 空间四滑杆 / 动作按钮 / 截图 / 10 秒序列；`?brain=off` 只看月亮，`?panel=off` 收起面板
- [x] 无头验收管线：`__step(秒)`、`__snapSave()`、`__recordGif("cp0")` → `python3 scripts/make-gif.py cp0`（15fps、540×960、10.5s，≈5MB）
- [ ] 嘴 + 腮红常驻（2026-09-23 拍板）：`params.ts` 已有 `mouth` / `blush` 两节（参考图比例、微笑 / o 型 / 平三种嘴形、腮红随情绪加深），渲染层接线待做；眼睛默认已改成参考图比例（0.49 / 0.54 / 0.14）；发光默认改为光晕 + 地照、表面自发光封顶 0.2（`light.selfGlow*`、`halo*`），面板加嘴形 / 腮红 / 自发光滑杆
- [x] 脸（2026-09-23 look-dev 拍板接线）：嘴三形态（微笑 / o / 平）弹簧插值、由情绪或动作决定；腮红常驻随开心 / 害羞加深；比例按参考图（`params.mouth` / `params.blush`）
- [x] 月面：NASA 柔和贴图 2k 副本（`web/public/moon/`，350 KB）+ 高程转法线凹凸 + 表面自发光（封顶 0.2）+ 光晕默认档 soft 1.0；`surfaceRealism` 滑杆 0 = 光面
- [x] 远星几乎不动：每层 `parallax` 系数，最远层 0.05（用户 2026-09-23）
- [ ] 真机陀螺仪：代码已接（iOS 需手势授权按钮），但需要 HTTPS 才能在手机上测——P4 做
- [ ] 手机 Safari ≥30fps 未实测（帧率上限已按触屏设备设 30）
- ⏸ **CHECKPOINT 0**（打开页面，盯 3 分钟，再点几个动作）：
  ① 待机 3 分钟不觉得死板吗？ ② 眨眼有"性格"吗（不是机械定时）？ ③ 停下时的压扁弹回可爱还是奇怪？ ④ 把月相滑到蛾眉月：地照下的暗盘好看吗、还像它吗？ ⑤ 眼睛的位置和间距对吗（拖滑杆找到你的答案，我记下来当默认值）？
  **附加**：录一段 10 秒 GIF 给 5 个人看，3 人笑 = 过。
  **GIF 夹具（已做）**：序列 = 待机眨眼 1.5s → 飘近 → 弹两下（^ ^）→ 歪头想想 → 转过去（眼睛到背面）→ 亮起（^ ^），全程镜头自动摇给视差；竖屏 540×960、15fps、10.5s、夜里 22 点。录法：打开 `http://localhost:5176/?brain=off`，console 里 `await __recordGif("cp0")`，再 `python3 scripts/make-gif.py cp0` → `snaps/cp0.gif`。序列表在 `web/main.ts` 的 `DEMO`，改了表 GIF 就跟着变。发给人时只发 GIF，不解释。

## P1 · 接通大脑 + 低延迟
目标：跟它打字说话，表情先于文字到达；每一轮的延迟都看得见。
Definition of Done：
- `npm run test:persona` 20/20（硬断言 + LLM 评审）
- 面板显示 first_emotion / first_text / done；连续 10 轮 first_emotion p50 ≤ 1.5s、first_text p50 ≤ 2.5s（Haiku 级模型）
- Tier 0 反射全部接到真月亮上（打字→飘近、思考→歪头、卡住→变暗、断线/重连、出错→抖）

- [ ] MoonRenderer 接入运行时（替换 PlaceholderRenderer），情绪惯性 τ 可调
- [ ] 人格 v1：用户改写 `persona/system-prompt.md` 草案（名字、口头禅、它的生活）；agent 跑测例修漂移
- [ ] 延迟采样：面板显示最近 10 轮的 p50/p90，写进会话记录
- [ ] 模型路由：默认 Haiku 级；`.env` 一行可切 provider/model
- [ ] 💡 Tier 0.5：用户发送前对文字做本地粗判（emoji / 关键词 → 情绪预置），零延迟先摆姿态；可关
- ⏸ **CHECKPOINT 1**：① 打字时它飘近了吗、停手时退回去了吗？ ② 说一句难过的话，它先暗下来再出字吗？ ③ 连聊十轮，它出戏 / 说教了吗？ ④ 延迟数字你能接受吗？ ⑤ 人格草案里哪一段最不像你想要的它？

## P2 · 记忆与主动性
目标：跟它说"明天要面试有点紧张"，第二天打开它主动问起——用户起鸡皮疙瘩。
Definition of Done：
- follow-up 全链路：整理器提取 → 到期 → 开屏主动提起 → 频控 ≤2/天 → 过期 3 天作废
- 久别 ≥3 天归来话术是月亮自己的（圆了又缺）
- `npm run memory -- list/add/due/seen` 可用

- [ ] 记忆链路在本仓库跑通（已复制，需验证：embedding 模型下载、SQLite 路径、整理定时）
- [ ] 主动开口时的身体：brighten + 星星先动一下再出字（P3 前先用气泡）
- [ ] 记忆的"月亮式"表达：清楚记得 / 模糊记得 → 光的亮度或眨眼慢一点（渲染层信号，可选）
- [ ] 危机兜底话术与用户共同定稿
- ⏸ **CHECKPOINT 2**：① 昨天说的事它今天问了吗？ ② 问的口气像它吗（不像客服）？ ③ 一天里被主动打扰的次数烦吗？ ④ 离开 3 天回来它说的话让你想笑还是想翻白眼？

## P3 · 回应通道：星星拼字
目标：重要的话由星星拼出来；日常的话走气泡；两者切换不突兀。
Definition of Done：
- 星星粒子系统（≥600 粒）60fps；手写体 outline 采样；弹簧收敛 + stagger；拼完停留 → 散开回夜空
- 通道选择规则写在代码里（由头里的 intensity / proactive / 句长决定），可调
- 气泡与拼字不同时出现

- [ ] 字形 outline 采样（opentype.js）+ 中文手写字体选型（授权可商用）
- [ ] 粒子目标分配（匈牙利 / 最近邻贪心）+ 弹簧收敛
- [ ] 通道规则 + 参数外置
- [ ] 💡 备用通道「起雾擦字」原型（可选，一晚上）
- ⏸ **CHECKPOINT 3**：① 拼字过程好看吗、太慢吗？ ② 什么样的话该拼、什么样的话该气泡——规则符合直觉吗？ ③ 拼完散开的方式对吗？ ④ 起雾要不要？

## P4 · 空间感与重力感应
目标：倾斜手机像透过窗户往一个有深度的盒子里看；月亮有重量。
Definition of Done：
- 离轴投影相机；星星 ≥3 层深度视差；空气透视；DeviceOrientation 低通 + 弹簧；iOS 权限引导页
- 月亮相对相机有弹簧滞后（可调）

- [x] 渲染栈决定：裸 three.js（2026-09-22 已拍板，见「已解决」）
- [ ] 离轴投影 + 多层星空 + 空气透视 + 近景 bokeh（P0 先做粗版，本阶段打磨）
- [ ] 重力感应真机：iOS 权限引导页、HTTPS（自签证书或隧道）、低通 + 弹簧；桌面端鼠标模拟在 P0 已有
- [ ] 边缘处理：极淡暗角 + 随倾斜滑动的玻璃反光（A2，见 design.md §5）
- ⏸ **CHECKPOINT 4**：① 倾斜时像窗户还是像贴图？ ② 月亮的"重量"对吗？ ③ 晕吗（10 分钟）？ ④ 桌面端鼠标模拟够用吗？ ⑤ 这就是你脑海里那个效果吗——差在哪？

## P5 · 天空、时间与天气
目标：它的世界跟着真实世界走。
Definition of Done：
- 太阳高度角驱动背景（纬度来自系统时区粗估或定位授权）；真实月相；天气 API → 情绪基线（mood floor）
- 阴天 = 云层 + 柔雾，永不完全消失；极光稀有事件；流星事件 → 陨石坑（外围环带）

- [ ] 太阳高度角 / 月相的本地算法
- [ ] 天气 provider（Open-Meteo）+ 情绪基线 + mood floor
- [ ] 云层 / 柔雾 / 极光 / 流星 shader
- [ ] 陨石坑生成与"月尘填平"生命周期（参数外置）
- ⏸ **CHECKPOINT 5**：① 你所在的时刻，天的颜色对吗？ ② 阴天的它还"在"吗？ ③ 极光出现的频率对吗？ ④ 流星砸出坑的那一下，你心疼还是开心？

## P6 · 成长：画花纹
目标：用户在月亮上画的东西会留下来。
Definition of Done：
- 触屏绘制到 UV 贴图（笔刷 / 颜色 / 撤销）；持久化到本地；坑被画过 → 永久
- UGC 默认仅自用；分享另审（先不做分享）

- [ ] 绘制层 + 持久化
- [ ] 与陨石坑生命周期联动
- [ ] 💡 官方花纹投送的数据格式（先留接口）
- ⏸ **CHECKPOINT 6**：① 画的手感？ ② 第二天打开它还在吗、还像你画的吗？ ③ 你想给它画什么？

## P7 · 声音
- [ ] 自适应氛围层（drone + 音阶随机单音，随情绪换音阶）
- [ ] 《月光》彩蛋（soundfont 自合成；琴键 UI；星星落键）
- ⏸ CHECKPOINT 7：① 开着声音待 10 分钟烦吗？ ② 彩蛋触发条件对吗？

## P8 · 产品化（轮廓，细节等 P0~P3 过了再写）
Capacitor 打包 iOS；推送（follow-up）；订阅 + 道具（RevenueCat）；成本模型重算（每 DAU LLM 成本 vs 定价）；商店材料；TestFlight beta ≥20 人；隐私说明（记忆本地）。

---

## 未决设计问题（human 待拍板）
1. **角色名**（用户 2026-09-22：候选记下，自己再想）。中文候选：**小满**（"一点点满"，永远差一点才圆；「小满胜万全」和「它不懂大道理」是同一种性格；反面：节气名 + 常见商标，如阿里小满 CRM、小满茶田，商店独占性弱）；**皎皎**（「皎皎空中孤月轮」，独一无二、读音亮；反面：形容词感强、性格感弱，要靠眼神补）。英文候选：**Mochi**（软、圆、会压扁，和 ±4% squash 一致；反面：宠物类 app 撞名多）；**Luma**（光；反面：Luma AI 撞名）。单用 Bao 撞 Pixar《Bao》。代码里用 `CHARACTER_NAME` 环境变量，定名前不阻塞任何阶段。
4. **首发市场与语言**：海外先行（中国区需备案模型 API）？中英双语人格各一份还是先中文？
5. **回应通道默认比例**：什么算「重要时刻」值得拼字——由 intensity 阈值、主动开口、句长决定；阈值你调。
6. **前摄人脸驱动视差**：效果更强 vs 隐私/电量/权限弹窗。agent 倾向先不做，重力感应够用。
7. **天气 API**：Open-Meteo（免费、无 key、需定位或城市）。是否请求定位权限，还是让用户手填城市？
8. **陨石坑参数**：流星频率、坑的寿命、月尘填平速度——先给默认，你在 P5 调。
9. **共享代码策略**：已从姊妹项目**复制而不共享**；记忆层的改进手动同步。要不要以后抽 companion-core，等两个仓库都稳定再说。
10. **性能预算**：移动端渲染 30fps 上限、后台暂停、粒子数上限——P0 定基线。
11. **真实月面 vs 光面**（2026-09-23 look-dev 支线）：`tools/blender/moon_lookdev.py` 出了程序化 / NASA LRO 两种月面 × 柔和 × 脸 × 三种发光，素材归档 `assets/moon/`（大贴图 gitignore，一分钟可重生成；高程 / 反照率 PNG 可直接喂 three.js displacement / color）。看过对照后的建议：用 NASA 柔和 0.5 的贴图 + 光晕，脸区不用压平（豆眼是画面里唯一纯黑，不打架）。要不要在 P0 就上真实贴图、还是先光面，CHECKPOINT 0 时拖 `surfaceRealism` 滑杆定。
12. ✅ **嘴和腮红常驻**（2026-09-23 用户拍板：「它不需要说话，但可以有 o 型嘴等表情」）。→ P0 的 `params.ts` 加嘴 + 腮红参数与嘴形词汇（微笑 / o 型 / 平），比例见 docs/design.md §2。

已解决：
- 2026-09-22 品牌名 **Moonbao**，仓库 `heyaozh/moonbao`。
- 2026-09-22 三个角色三个独立 app、串行、共用代码库。
- 2026-09-22 放弃完全刚体，加 ±4% squash & stretch。
- 2026-09-22 月相 = 光照不是几何；地照保证永不完全看不见。
- 2026-09-22 成长机制 = 用户画花纹；**陨石坑不由对话内容触发**。
- 2026-09-22 它不说话；回应用星星拼字（重要时刻）+ 气泡（日常）。
- 2026-09-23 **空间感三旋钮**：窗 = 屏幕本身无实体框（A2）；月亮中景「隔着屏幕」一臂远、会飞远飞近、最近时一只眼特写；倾斜要看得出在动，20° ≈ 横移 1/4 屏宽；星星可从月亮前面飞过；「穿过去」进 backlog。细节 docs/design.md §5。
- 2026-09-22 **渲染栈 = 裸 three.js**（用户：技术上难判断，按 agent 方案走）。理由：前端是 vanilla TS + 事件总线，没有 React；「透过窗户看盒子」的核心是离轴投影，是相机 projectionMatrix 的十几行，不依赖 drei；bloom 等用 three 自带 EffectComposer。记录过的路线：① 裸 three.js（选定）；② R3F + drei（MeshPortalMaterial 现成、pmndrs 示例可直接抄，代价是为渲染层引入 React 整个架构税）；③ 裸 three + 手写 stencil portal（若以后真要「穿过去」，在 ① 上加，不换栈）。渲染层协议隔离，最坏情况 P4 重写一次渲染层。
- 2026-09-22 **Gate = 2027-01-31**。moonbao 顶替暂停中的 Monstella 占「商业赌注」槽位（全局档案 §5 已同步）。标准：① **2026-10-10 前**过 CHECKPOINT 0「5 人 3 笑」；② 2027-01-15 前 **20 个外部 beta，渠道不限**（网页链接 / PWA 也算，Capacitor 上架不是前置条件）；③ D7 留存 ≥25%；④ API 成本免费用户 ≤€0.05/月、付费 ≤€2.5/月；⑤ 至少一条可传播内容（星星拼字 10 秒视频）发出去。过 → P8 产品化；不过 → 停。已知挤压：10 月上旬同时要验收 CP0 和婚礼视频（婚礼由 Codex 开发，每日 digest 报账）；CP0 若 10-10 没过就顺延到 11 月底回国后。

## 本轮假设（agent 自行决定的可逆默认）
- 角色名默认 `CHARACTER_NAME=小满`，人格与测例用 `{{NAME}}` 占位，一行环境变量切换。
- 渲染层先放 DOM 占位圆，只为今天就能验证整条链路；P0 换成裸 three.js。
- 动作词汇 12 个（见 protocol.ts），与旧词汇一一对应但全部换成「位移/翻滚/光」语义；LLM 人格协议同步更新。
- 默认模型 `claude-haiku-4-5-20251001`（快档起步；人格测例过了再考虑换）。
- 前端端口 5176（避开姊妹项目的 5175，两个可同时开）。
- 移除了语音 VAD 的静态资源拷贝（它不说话；语音输入仍可用 `VOICE_INPUT=on` 走 whisper，只是前端没接麦克风）。

## Backlog（以后再说）
真实星空贴图（按所在地与时间投影 HYG 星表到最远层，design.md §4）· portal「穿过去」· 起雾擦字通道 · 前摄视差 · 官方花纹投送 · 花纹分享与审核 · 桌宠（Tauri）· 真人语音付费档 · 多语言人格 · 星星命名/收集 · 与其它角色的"窗台"平台化。

## 会话记录（只增不改）
- 2026-09-22：立项。品牌名定 Moonbao（查过：英文侧干净，中文「月宝」有一款母婴 app，moonbao.com 被域名贩子挂售 $4,195 别买）。从姊妹项目复制大脑层（协议 / 级联引擎 / 记忆 / 节拍器 / 运行时 / 人格测例结构），角色引用全部清除；动作词汇改为月亮版；人格 v0 草案由 agent 起草待用户推翻；DOM 占位渲染器跑通全链路（`/api/health` OK、vite 200、typecheck 0 错）。写 `docs/design.md`（设定与想法）、本文件、`docs/next-session-prompt.md`。**用户下一步**：refine 本计划；拍板未决问题 1~3；决定何时开 P0。
- 2026-09-22（第二场）：过 PLAN。拍板：渲染栈裸 three.js；gate 2027-01-31、beta 渠道不限；moonbao 顶 Monstella 占商业赌注槽位。角色名候选记入未决 1，用户自己想。接着逐题讨论功能 / 视觉 / 互动（结果写入本文件或 docs/design.md）。
- 2026-09-23：空间感拍板（见已解决）。用户要求先做 P0 视觉 demo（不接对话），并确认桌面端可用鼠标模拟倾斜。
- 2026-09-23（P0 demo，PR #3）：裸 three.js 月亮落地：`web/moon/{params,math,camera,eyes,blink,actions,sky,renderer}.ts`。窗相机离轴投影、三层远星 + 前景尘埃、月相 shader + 地照 + 光晕、球面豆眼 + 四形态眨眼、三条弹簧 + noise 漂浮 + squash、12 动作原语 + 特写 / 飞远。无头管线 `__step / __snapSave / __recordGif` + `scripts/make-gif.py`，第一版 cp0.gif 已出。agent 自检发现并修掉：特写距离公式错、光晕放在月心后面导致倾斜时错位、蛾眉月地照过亮盖住月牙。**待用户验收 CHECKPOINT 0**（桌面：鼠标 = 倾斜；真机陀螺仪要 HTTPS，P4）。已知观感问题留给用户判断：竖屏里月亮偏小（深度滑杆可调）、蛾眉月的月牙偏细（terminatorSoftness 可调）。
- 2026-09-23（look-dev 支线，另一工作树）：Blender 5.1 无头管线 `tools/blender/moon_lookdev.py`，素材 `assets/moon/`。拍板：发光 = 光晕 + 地照、表面自发光封顶 0.2、默认档 soft 1.0；嘴和腮红常驻、可有 o 型嘴等表情。新增未决 11。支线收尾，回主线 P0。
- 2026-09-23（P0 脸接线）：把 look-dev 拍板接进 three.js：嘴 / 腮红（`web/moon/face.ts`）、NASA 柔和贴图 + 凹凸 + 自发光（shader）、眼睛改参考图比例、光晕 soft 1.0；远星改为几乎不动（每层 parallax）。用户提的「真实星空贴图」进 backlog + design.md §4。面板加表情按钮与月面 / 光滑杆。GIF 重录。
