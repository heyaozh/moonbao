# moonbao · agent 入口

一个住在你手机夜空里的小月亮。它不说话，用眼神、光和星星回应你；它记得你说过的话，第二天会想起来问你。

**开工前按顺序读**：本文件 → `PLAN.md`（先看会话记录末尾，再找第一个未完成阶段）→ `docs/design.md`（设定与想法的完整记录）。
协议：`~/Development/loop-engineering.md`（PLAN.md 单一事实来源、Definition of Done、⏸CHECKPOINT ≤5 问、参数外置、反馈原文入日志、已拍板不翻案）。

## 三个侧重点（做取舍时按这个顺序）
1. **动画 / 可爱度**——P0 灭火，不过不进 P1。可爱来自眼神、运动曲线（ζ≈0.4~0.6 的过冲）、光，不是几何复杂度。
2. **低延迟**——Tier 0 纯代码反射（0ms）→ 首行头流式抢跑 → 节拍器。每轮都量 first_emotion / first_text。
3. **记忆与主动性**——follow-up 主动提起是留存的第一杠杆；频控每日 ≤2 次是铁律。

## 硬规则
- 渲染层可替换：角色运行时只输出 `{情绪, 动作意图, 文本}`；任何渲染实现只消费 `shared/protocol.ts`。
- 温柔设计红线：被冷落/恶语只有短期反应，快速原谅；绝不枯萎、绝不愧疚驱动留存。**陨石坑不由负面对话触发。**
- 它不懂大道理：不给建议除非被问，永不说教，绝不用列表/助手腔。人格改动前后都跑 `npm run test:persona`。
- 参数外置：human 可能想调的数值集中到一处并加注释（P0 起在 `web/moon/params.ts`）。
- 新想法一律进 PLAN.md「未决设计问题 / backlog」，不进当前阶段。
- Git：分支前缀 `claude/`、`codex/`；一功能一 PR；不合并 PR（合并归用户）；不用 `git stash`；不提交 `.env`。
- 花钱、部署、对外发布、删除不可恢复的数据：先问。

## 起源
大脑层（协议 / 级联引擎 / 记忆 / 节拍器 / 人格测例结构）从姊妹项目复制而来，复制而不共享；本仓库不再引用它的角色设定。
