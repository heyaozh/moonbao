# 开新 session 用的交接 prompt

> 立项那次对话已归档，讨论内容全部沉淀在仓库文件里。新 session 从文件恢复，不从聊天恢复。
> 把下面 ``` 里的整段粘进去。`~/.claude/CLAUDE.md` 会自动加载，所以「我是谁 / 怎么合作」不用重复。

```
这是 moonbao（~/Development/moonbao）：一个住在手机夜空里的小月亮陪伴 app。先读 CLAUDE.md → PLAN.md → docs/design.md，读完再说话。

立项 session 已归档，别问我"之前讨论过什么"——都在 docs/design.md 里。品牌名 Moonbao 已定；中文角色名在 小满 / 皎皎 之间，我还没定。

这次我想先把 PLAN.md 过一遍再开工。请你：

1. 先把 PLAN.md「未决设计问题」的 1~3 挑出来，每个给我一句话的建议和它的反面理由，我来拍板。拍板后写进「已解决」，从此不翻案。
2. 然后按下面这些开放点跟我讨论——一次一个，别把十个问题一起抛给我。讨论结果写进 PLAN.md 或 docs/design.md，不留在聊天里。
3. 讨论完了再进 P0。P0 的第一件事是 web/moon/params.ts，把所有可调数值先摆出来给我看。

## 还开放的点（按我在意的顺序）

**空间感**：我脑海里有一个效果，类似 https://pmndrs.github.io/examples/examples/pass-through-portals ——月亮在一个有深度的空间里，手机倾斜时像透过窗户往盒子里看。docs/design.md §5 记了术语（离轴投影、fish tank VR、multiplane、空气透视）。我描述不清楚的地方请你用问题引导我：它离窗口多远？星星在它前面还是后面？倾斜的幅度对应多大的视角变化？要不要"穿过去"？

**渲染栈**：R3F + drei 还是裸 three.js。这个最好 P0 前就定，否则 P4 要重写渲染层。

**可爱度的验收夹具**：CHECKPOINT 0 要 5 人看 GIF 3 人笑。GIF 怎么录、录什么动作序列、多长——请你设计好，我只负责发给人。

**低延迟**：P1 的目标 first_emotion p50 ≤ 1.5s。请你先估一下用 Haiku 级模型 + prompt caching 大概能到多少，以及 Tier 0.5（发送前本地粗判情绪）值不值得做。

**记忆的月亮式表达**：清楚记得 / 模糊记得，能不能变成光或眨眼的差别，而不只是措辞？

**星星拼字**：中文手写字体选型（要可商用授权）；什么话该拼、什么话该气泡；拼完怎么散开。

**陨石坑的生命周期**：流星多久来一次、坑多久被月尘填平、画过的永久——给我默认值和调参位置。

**声音**：氛围层用什么音阶；《月光》彩蛋的触发条件；soundfont 从哪来（授权）。

**发布**：海外先行还是中国区；人格要不要中英双份；Capacitor 什么时候引入。

**性能预算**：手机上 30fps 上限、粒子数上限、后台暂停、电量——P0 定基线。

**名字**：小满 vs 皎皎，以及英文角色名。你可以给我你的倾向，但这个我自己定。

## 规矩
- 新想法一律进 PLAN.md 未决问题或 backlog，不进当前阶段。
- 不要引用姊妹项目的角色设定；大脑层代码是复制来的，但这个仓库只认月亮。
- 我说"头脑风暴"才展开新方向；没说就是建造模式。
```
